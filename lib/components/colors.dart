import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromARGB(255, 101, 59, 255); // Your primary app color
  static const accentColor = Color.fromARGB(255, 161, 84, 255); // Your accent color
  static const textColor = Colors.black; // Text color
  static const backgroundColor = Colors.white; // Background color
  // ... Add more colors as needed
}
